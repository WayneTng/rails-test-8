= simple_form_for @search, url: root_path, html: { method: :get }, remote: true do |form|
  .brands Brands
  = form.input :brand_ids, as: :check_boxes, collection: Brand.all, label: false
  = form.button :submit
